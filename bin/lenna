#!/usr/bin/env ruby
# frozen_string_literal: true

require 'lenna/cli/app'
require 'lenna/cli/commands/start_server'

# Call the CLI to start the server
#
# @private `Since v0.1`
#
Lenna::Cli::App.new(ARGV).execute(Lenna::Cli::Commands::StartServer)
